export function formatUser(username, age, status) {
  return `Hello! You are ${username}. Your age is ${age} years old. Status = ${status}.`;
}

export function getUserStatus(status) {
  return status;
}
